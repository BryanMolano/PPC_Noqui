<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Noqui">
        <activity
            android:name=".Servicios"
            android:exported="false" />
        <activity
            android:name=".enviar"
            android:exported="false" />
        <activity
            android:name=".boton_azul"
            android:exported="false" />
        <activity
            android:name=".caja_fuerte"
            android:exported="false" />
        <activity
            android:name=".activity_tarjeta_movimientos"
            android:exported="false" />
        <activity
            android:name=".activity_congelar_tarjeta"
            android:exported="false" />
        <activity
            android:name=".activity_tarjeta"
            android:exported="false" />
        <activity
            android:name=".Movimientos"
            android:exported="false" /> <!-- Login será la pantalla inicial -->
        <activity
            android:name=".Login"
            android:exported="true"> <!-- OBLIGATORIO si tiene intent-filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity ya no será inicial -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>